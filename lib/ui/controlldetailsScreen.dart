import 'package:flutter/material.dart';

class ControllerDetailsScreen extends StatelessWidget {
  const ControllerDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
